fun main() {

    val dado = readLine()

    // Garante que existe valor na leitura
    if (dado != "" && dado != null) {

        // Conversão de tipos
        dado.toInt()
        dado.toFloat()
        dado.toBoolean()

    }

}